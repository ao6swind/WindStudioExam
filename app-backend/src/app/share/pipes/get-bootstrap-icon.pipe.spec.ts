import { GetBootstrapIconPipe } from './get-bootstrap-icon.pipe';

describe('GetBootstrapIconPipe', () => {
  it('create an instance', () => {
    const pipe = new GetBootstrapIconPipe();
    expect(pipe).toBeTruthy();
  });
});
