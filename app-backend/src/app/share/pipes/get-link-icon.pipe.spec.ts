import { GetLinkIconPipe } from './get-link-icon.pipe';

describe('GetLinkIconPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLinkIconPipe();
    expect(pipe).toBeTruthy();
  });
});
