import { GetQuestionTypePipe } from './get-question-type.pipe';

describe('GetQuestionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GetQuestionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
