import { GetContentViewPipe } from './get-content-view.pipe';

describe('GetContentViewPipe', () => {
  it('create an instance', () => {
    const pipe = new GetContentViewPipe();
    expect(pipe).toBeTruthy();
  });
});
