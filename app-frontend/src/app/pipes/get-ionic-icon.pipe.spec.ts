import { GetIonicIconPipe } from './get-ionic-icon.pipe';

describe('GetIonicIconPipe', () => {
  it('create an instance', () => {
    const pipe = new GetIonicIconPipe();
    expect(pipe).toBeTruthy();
  });
});
